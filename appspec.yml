version: 0.0
os: linux

files:
  - source: /
    destination: /var/www
    overwrite: yes
permissions:
  - object: /var/www/
    owner: ec2
    group: ec2
hooks:
  AfterInstall:
    - location: script/deploy.sh
      timeout: 60
      runas: root
